# Contributing

This repository concerns Windows packaging. Report launcher/build reproducibility problems here. Scientific/model changes belong in PaedDeadSpace-Core, and UI behavior belongs in PaedDeadSpace-UI.

When reporting a Windows issue, include Windows version, Python version used for building (if relevant), the build log, and the SHA-256 of the affected asset.
